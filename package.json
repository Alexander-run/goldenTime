{
  "name": "horseSign",
  "version": "1.0.0",
  "description": "A simple sign app for Horse riding",
  "scripts": {
    "dev": "pnpm recursive --stream run dev",
    "build": "sh ./customizedBuild.sh",
    "build:front": "pnpm -r --filter=./frontend/* run build",
    "build:service": "pnpm -r --filter=./service run build"
  }
}